export default ({
    context: {
        lastCustomer,
        customerShortName,
        year,
        years,
        notifications,
    },
}) => ({
    lastCustomer,
    customerShortName,
    year,
    years,
    notifications,
});